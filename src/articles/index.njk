---
layout: base.njk
title: Articles techniques
permalink: /articles/
lang: fr
eleventyExcludeFromCollections: true
---

<section class="page">
  <header class="page-header">
    <h1 class="page-title">{{ title }}</h1>
  </header>

  <div class="section-intro">
    <p>{{ t.sections.articlesIntroP1 }}</p>
    <p>{{ t.sections.articlesIntroP2 }}</p>
  </div>

  {% set articlelist = collections["articles"] | filterByLang("fr") %}
  {% if articlelist.length %}
  <ul class="post-list">
    {% for post in articlelist | reverse %}
    <li>
      {% include "components/post-card.njk" %}
    </li>
    {% endfor %}
  </ul>
  {% else %}
  <p class="no-posts">Aucun article pour le moment.</p>
  {% endif %}
</section>
